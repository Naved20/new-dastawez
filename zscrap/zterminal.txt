# Create virtual environment
python -m venv venv

# Activate virtual environment
# On Windows:
venv\Scripts\activate
# On Linux:
source venv/bin/activate


install flask app {
    pip install flask
}

for remove git main origin {
    git remote remove origin
    rm -r -fo .git
}


for generate the secret key {
    python -c "import secrets; print(secrets.token_hex(32))"

}


for mongo db {
    python -m pip install "pymongo[srv]"
}




folder structure {
    flask-dastawez/
    ├── app.py                    # Main Flask application (Entry point)
    ├── .env                      # Environment variables
    ├── requirements.txt          # Python dependencies
    ├── vercel.json              # Vercel deployment config
    ├── database/
    │   └── mongo.py             # MongoDB connection setup
    ├── models/
    │   └── user_model.py        # User data model/class
    ├── services/
    │   └── user_service.py      # Business logic & database operations
    ├── routes/
    │   └── user_routes.py       # API routes/endpoints
    ├── templates/               # HTML templates
    ├── static/                  # CSS, JS, images
    └── .venv/                   # Virtual environment
}